# ヘルプセンター改善提案書

**作成日**: 2026-01-22
**対象**: ザイロク ヘルプセンター機能の全面刷新

---

## 📋 目次

1. [現状の課題](#現状の課題)
2. [解決策の提案](#解決策の提案)
3. [実装手順](#実装手順)
4. [必要な画像リスト](#必要な画像リスト)
5. [新デザイン仕様](#新デザイン仕様)
6. [技術的実装詳細](#技術的実装詳細)

---

## 現状の課題

### 1. データソースの問題
- ❌ `content/manual/`の限定的なMDXファイルのみ表示
- ❌ `docs/manual/`（66件）と`docs/qa/`（45件以上）の充実したコンテンツが未活用
- ❌ 公開ページ用（`00_public`）とスタッフ用（`01_staff`）の区別が曖昧

### 2. UI/UXの問題
- ❌ デザインが見づらく、情報が頭に入ってこない
- ❌ モバイル対応が不十分
- ❌ 検索機能が限定的（Q&Aに検索機能なし）
- ❌ パンくずリストやカテゴリ分類が不明瞭

### 3. 画像の問題
- ❌ スクリーンショットが用意されていない
- ❌ 画像サイズの調整機能がない
- ❌ 画像の保存先が統一されていない

---

## 解決策の提案

### ✅ 1. データソースの統合

#### 実装内容

**現状**:
```typescript
// lib/manual/metadata.ts (85行目)
const manualDir = path.join(process.cwd(), 'content', 'manual')
const qaDir = path.join(process.cwd(), 'content', 'qa')
```

**改善後**:
```typescript
// lib/manual/metadata.ts
const manualDir = path.join(process.cwd(), 'docs', 'manual')
const qaDir = path.join(process.cwd(), 'docs', 'qa')
```

#### MD形式の対応

**重要**: 現在の実装は既に`.md`ファイルに対応しています！

```typescript
// lib/manual/metadata.ts (70行目)
} else if (item.endsWith('.mdx') || item.endsWith('.md')) {
  files.push(fullPath)
}
```

**必要な作業**:
1. `docs/manual/`と`docs/qa/`のMDファイルにFrontmatterを追加
2. Frontmatterの形式を統一

#### Frontmatter形式の定義

**docs/manual/用の標準Frontmatter**:
```yaml
---
title: "ログイン方法"
description: "ザイロクへのログイン手順を説明します"
permission: 0  # 0=公開, 1=staff, 2=leader, 3=manager, 4=admin
plans: ["basic"]  # basic, asset_pack, dx_pack
category: "manual"  # manual or qa
tags: ["ログイン", "基本操作"]
lastUpdated: "2026-01-22"
---
```

**docs/qa/用の標準Frontmatter**:
```yaml
---
title: "ログインできない場合の対処法"
description: "ログイン時のトラブルシューティング"
permission: 0
plans: ["basic"]
category: "qa"
tags: ["ログイン", "トラブルシューティング"]
lastUpdated: "2026-01-22"
---
```

---

### ✅ 2. 新デザイン仕様

#### 2-1. ヘルプセンタートップページ

```
┌─────────────────────────────────────────────────────┐
│  🔍 検索バー（大きく目立つ）                          │
│  「何をお探しですか？」                               │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│  🚀 クイックスタート（カード形式）                    │
│  ┌──────┐  ┌──────┐  ┌──────┐  ┌──────┐          │
│  │ 📱   │  │ 🔧   │  │ 💰   │  │ ⚙️   │          │
│  │ログイン│  │道具管理│  │請求書 │  │設定  │          │
│  └──────┘  └──────┘  └──────┘  └──────┘          │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│  📚 カテゴリ別マニュアル（タブ切り替え）              │
│  [全て] [基本操作] [道具管理] [勤怠] [書類] [設定]   │
│                                                     │
│  ┌─────────────────────────────────────────────┐   │
│  │ 📄 ログイン方法                              │   │
│  │ ログイン手順と初回パスワード設定             │   │
│  │ 🏷️ ログイン・基本操作   📅 2026-01-22      │   │
│  └─────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────┐   │
│  │ 📄 QRスキャン                                │   │
│  │ QRコードを使った道具の持ち出し・返却         │   │
│  │ 🏷️ QR・道具管理   📅 2026-01-21            │   │
│  └─────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│  💡 Q&A（よくある質問）                              │
│  🔍 Q&A検索バー                                     │
│                                                     │
│  ▼ ログイン・アカウント (12件)                       │
│  ▼ 道具管理 (8件)                                   │
│  ▼ 勤怠管理 (15件)                                  │
└─────────────────────────────────────────────────────┘
```

#### 2-2. マニュアル詳細ページ

```
┌─────────────────────────────────────────────────────┐
│  ホーム > マニュアル > ログイン方法                   │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│  # ログイン方法                                      │
│  ログイン手順と初回パスワード設定を説明します         │
│  📅 更新日: 2026-01-22  🏷️ ログイン・基本操作       │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│  📑 目次（サイドバー固定、スマホは折りたたみ）         │
│  1. ログイン画面へのアクセス                         │
│  2. ログイン手順                                     │
│  3. 初回ログイン時の注意点                           │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│  ## 1. ログイン画面へのアクセス                      │
│                                                     │
│  ブラウザで以下のURLにアクセスしてください:           │
│  https://あなたの会社名.zairoku.com                 │
│                                                     │
│  [スクリーンショット: ログイン画面（PC）]            │
│  ↑ 画像をクリックで拡大表示                         │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│  💡 関連記事                                         │
│  • パスワードを忘れた場合                           │
│  • 初回セットアップ                                 │
│  • よくあるログインエラー                           │
└─────────────────────────────────────────────────────┘
```

#### 2-3. Q&Aページ

```
┌─────────────────────────────────────────────────────┐
│  💡 Q&A - よくある質問                               │
│  🔍 質問を検索（リアルタイム検索）                    │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│  📂 カテゴリ別Q&A                                    │
│                                                     │
│  ▼ ログイン・アカウント (12件)                       │
│    Q. ログインできない場合はどうすればいいですか？    │
│    Q. パスワードを忘れた場合は？                     │
│    Q. アカウントがロックされました                   │
│                                                     │
│  ▼ 道具管理 (8件)                                   │
│    Q. QRコードがスキャンできません                   │
│    Q. 道具の移動履歴を確認するには？                 │
└─────────────────────────────────────────────────────┘
```

#### デザインの特徴

**カラースキーム**:
- プライマリ: `#2563EB` (Blue-600)
- セカンダリ: `#10B981` (Green-500)
- アクセント: `#F59E0B` (Amber-500)
- 背景: `#F9FAFB` (Gray-50)
- カード: `#FFFFFF` (White)

**タイポグラフィ**:
- 見出し: `font-bold text-2xl sm:text-3xl`
- 本文: `text-base leading-relaxed`
- キャプション: `text-sm text-gray-600`

**スペーシング**:
- カード間: `space-y-4`
- セクション間: `space-y-8`
- パディング: `p-6` (PC), `p-4` (Mobile)

**レスポンシブ**:
- ブレークポイント: `sm:640px`, `md:768px`, `lg:1024px`
- モバイルファースト設計

---

### ✅ 3. 検索機能の強化

#### 3-1. リアルタイム検索

**実装内容**:
- タイトル、説明、タグ、本文から検索
- 入力時に即座に結果を表示
- ハイライト表示

**技術スタック**:
```typescript
// lib/manual/search.ts
export function searchArticles(
  articles: ManualArticle[],
  query: string
): ManualArticle[] {
  const lowerQuery = query.toLowerCase()

  return articles.filter(article => {
    // タイトル検索
    if (article.frontmatter.title.toLowerCase().includes(lowerQuery)) {
      return true
    }

    // 説明検索
    if (article.frontmatter.description.toLowerCase().includes(lowerQuery)) {
      return true
    }

    // タグ検索
    if (article.frontmatter.tags.some(tag =>
      tag.toLowerCase().includes(lowerQuery)
    )) {
      return true
    }

    // 本文検索（HTML除去）
    const textContent = article.content.replace(/<[^>]*>/g, '')
    if (textContent.toLowerCase().includes(lowerQuery)) {
      return true
    }

    return false
  })
}
```

#### 3-2. Q&A専用検索

**実装内容**:
- Q&Aページに専用検索ボックス
- カテゴリフィルター
- 検索履歴の保存

---

### ✅ 4. 画像管理システム

#### 4-1. 画像保存ルール

**保存先**:
```
public/images/manual/
├── login/
│   ├── login-screen-pc.png (ログイン画面 - PC)
│   ├── login-screen-mobile.png (ログイン画面 - スマホ)
│   └── password-reset.png (パスワードリセット画面)
├── qr/
│   ├── qr-scan-step1.png (QRスキャン手順1)
│   ├── qr-scan-step2.png (QRスキャン手順2)
│   └── qr-print.png (QR印刷画面)
├── tools/
│   ├── tool-list.png (道具一覧)
│   ├── tool-register.png (道具登録画面)
│   └── tool-movement.png (道具移動履歴)
├── attendance/
│   ├── clock-in.png (出勤打刻)
│   ├── clock-out.png (退勤打刻)
│   └── attendance-list.png (勤怠一覧)
├── documents/
│   ├── estimate-create.png (見積書作成)
│   ├── invoice-create.png (請求書作成)
│   └── purchase-order.png (発注書作成)
└── settings/
    ├── organization-settings.png (組織設定)
    ├── staff-management.png (スタッフ管理)
    └── data-export.png (データエクスポート)
```

**命名規則**:
- 小文字、ハイフン区切り
- 内容が分かる名前
- 連番を使う場合: `-step1`, `-step2`

#### 4-2. 画像最適化

**推奨サイズ**:
- **フル幅画像**: 1200px × 自動（デスクトップ画面全体）
- **インライン画像**: 800px × 自動（説明画像）
- **アイコン・小画像**: 400px × 自動（UI要素）
- **モバイル専用**: 375px × 自動（スマホ画面）

**フォーマット**:
- **PNG**: UI画面（透過不要）
- **WebP**: 写真・複雑な画像（軽量化）
- **SVG**: アイコン・図形

**Markdown記法**:
```markdown
<!-- 通常サイズ -->
![ログイン画面](/images/manual/login/login-screen-pc.png)

<!-- サイズ指定（HTMLタグ使用） -->
<img src="/images/manual/login/login-screen-pc.png" width="700" alt="ログイン画面（PC）" />

<!-- モバイル専用 -->
<img src="/images/manual/login/login-screen-mobile.png" width="300" alt="ログイン画面（スマホ）" />
```

---

## 必要な画像リスト

### 優先度A（最重要 - 15枚）

#### ログイン・基本操作（3枚）
1. **login-screen-pc.png** - ログイン画面（PC版）
   - サイズ: 1200×800px
   - 内容: メールアドレス・パスワード入力画面

2. **login-screen-mobile.png** - ログイン画面（スマホ版）
   - サイズ: 375×667px
   - 内容: モバイルログイン画面

3. **password-reset.png** - パスワードリセット画面
   - サイズ: 800×600px
   - 内容: パスワードリセットフォーム

#### QRスキャン（4枚）
4. **qr-scan-camera.png** - QRスキャン画面
   - サイズ: 375×667px
   - 内容: カメラ起動中の画面

5. **qr-scan-success.png** - スキャン成功画面
   - サイズ: 375×667px
   - 内容: スキャン成功メッセージ

6. **qr-print-settings.png** - QR印刷設定画面
   - サイズ: 1200×800px
   - 内容: QRサイズ選択画面

7. **qr-code-sample.png** - QRコードサンプル
   - サイズ: 200×200px
   - 内容: 道具QRコードの例

#### 道具管理（4枚）
8. **tool-list.png** - 道具一覧画面
   - サイズ: 1200×800px
   - 内容: 道具一覧テーブル

9. **tool-register.png** - 道具登録画面
   - サイズ: 1200×800px
   - 内容: 道具登録フォーム

10. **tool-movement-history.png** - 移動履歴画面
    - サイズ: 1200×800px
    - 内容: 道具移動履歴テーブル

11. **tool-qr-bulk-print.png** - 一括QR印刷画面
    - サイズ: 1200×800px
    - 内容: QR一括印刷プレビュー

#### 勤怠管理（2枚）
12. **attendance-clock-in.png** - 出勤打刻画面
    - サイズ: 375×667px
    - 内容: 出勤ボタン押下後の画面

13. **attendance-list.png** - 勤怠一覧画面
    - サイズ: 1200×800px
    - 内容: 月次勤怠一覧

#### 書類管理（2枚）
14. **estimate-create.png** - 見積書作成画面
    - サイズ: 1200×800px
    - 内容: 見積書入力フォーム

15. **invoice-create.png** - 請求書作成画面
    - サイズ: 1200×800px
    - 内容: 請求書入力フォーム

### 優先度B（重要 - 10枚）

#### 設定・管理（4枚）
16. **staff-management.png** - スタッフ管理画面
17. **organization-settings.png** - 組織設定画面
18. **data-export.png** - データエクスポート画面
19. **warehouse-locations.png** - 倉庫位置管理画面

#### 作業報告書（2枚）
20. **work-report-create.png** - 作業報告書作成画面
21. **work-report-pdf.png** - PDF出力プレビュー

#### モバイル画面（4枚）
22. **mobile-bottom-nav.png** - モバイル下部ナビ
23. **mobile-sidebar.png** - モバイルサイドバー
24. **mobile-qr-scan.png** - モバイルQRスキャン
25. **mobile-dashboard.png** - モバイルダッシュボード

### 優先度C（あると良い - 5枚）

26. **consumable-management.png** - 消耗品管理
27. **equipment-maintenance.png** - 重機メンテナンス
28. **site-management.png** - 現場管理
29. **purchase-order-approval.png** - 発注書承認フロー
30. **audit-log.png** - 監査ログ

---

## 実装手順

### フェーズ1: データソース移行（1-2日）

#### Step 1: Frontmatterの追加
1. `docs/manual/`の全MDファイルにFrontmatterを追加
2. `docs/qa/`の全MDファイルにFrontmatterを追加

**作業スクリプト**:
```bash
# Frontmatter追加スクリプト（例）
#!/bin/bash
for file in docs/manual/*.md; do
  # ファイル名からタイトルを生成
  title=$(basename "$file" .md | sed 's/_/ /g' | sed 's/\b\(.\)/\u\1/g')

  # Frontmatterを追加
  cat > temp.md << EOF
---
title: "$title"
description: "TODO: 説明を追加"
permission: 1
plans: ["basic"]
category: "manual"
tags: []
lastUpdated: "2026-01-22"
---

$(cat "$file")
EOF

  mv temp.md "$file"
done
```

#### Step 2: metadata.tsの修正
```typescript
// lib/manual/metadata.ts
const manualDir = path.join(process.cwd(), 'docs', 'manual')
const qaDir = path.join(process.cwd(), 'docs', 'qa')
```

#### Step 3: 動作確認
- ヘルプセンターページで66件のマニュアルが表示されるか確認
- Q&Aページで45件以上のQ&Aが表示されるか確認

### フェーズ2: デザイン刷新（2-3日）

#### Step 1: 新コンポーネント作成

**components/HelpCenter/SearchBox.tsx**:
```typescript
'use client'
import { useState } from 'react'
import { MagnifyingGlassIcon } from '@heroicons/react/24/outline'

export function SearchBox({ onSearch }: { onSearch: (query: string) => void }) {
  const [query, setQuery] = useState('')

  return (
    <div className="relative">
      <MagnifyingGlassIcon className="absolute left-4 top-1/2 -translate-y-1/2 h-6 w-6 text-gray-400" />
      <input
        type="text"
        placeholder="何をお探しですか？（例: ログイン方法、QRスキャン）"
        className="w-full pl-12 pr-4 py-4 text-lg border-2 border-gray-200 rounded-xl focus:border-blue-500 focus:ring-2 focus:ring-blue-200 transition-all"
        value={query}
        onChange={(e) => {
          setQuery(e.target.value)
          onSearch(e.target.value)
        }}
      />
    </div>
  )
}
```

**components/HelpCenter/QuickStartCards.tsx**:
```typescript
import Link from 'next/link'

const quickLinks = [
  { icon: '📱', title: 'ログイン', href: '/manual/login', color: 'blue' },
  { icon: '🔧', title: '道具管理', href: '/manual/tool_management', color: 'green' },
  { icon: '💰', title: '請求書', href: '/manual/invoices', color: 'amber' },
  { icon: '⚙️', title: '設定', href: '/manual/settings', color: 'purple' },
]

export function QuickStartCards() {
  return (
    <div className="grid grid-cols-2 md:grid-cols-4 gap-4">
      {quickLinks.map((link) => (
        <Link
          key={link.href}
          href={link.href}
          className={`
            bg-white rounded-xl p-6 shadow-md hover:shadow-xl
            transition-all duration-200 hover:-translate-y-1
            border-2 border-${link.color}-100 hover:border-${link.color}-300
          `}
        >
          <div className="text-4xl mb-2">{link.icon}</div>
          <div className="font-semibold text-gray-900">{link.title}</div>
        </Link>
      ))}
    </div>
  )
}
```

**components/HelpCenter/CategoryTabs.tsx**:
```typescript
'use client'
import { useState } from 'react'

const categories = [
  { id: 'all', label: '全て', icon: '📚' },
  { id: 'basic', label: '基本操作', icon: '🚀' },
  { id: 'tools', label: '道具管理', icon: '🔧' },
  { id: 'attendance', label: '勤怠', icon: '⏰' },
  { id: 'documents', label: '書類', icon: '📄' },
  { id: 'settings', label: '設定', icon: '⚙️' },
]

export function CategoryTabs({ onCategoryChange }: { onCategoryChange: (id: string) => void }) {
  const [active, setActive] = useState('all')

  return (
    <div className="flex overflow-x-auto gap-2 pb-2 scrollbar-hide">
      {categories.map((cat) => (
        <button
          key={cat.id}
          onClick={() => {
            setActive(cat.id)
            onCategoryChange(cat.id)
          }}
          className={`
            flex items-center gap-2 px-4 py-2 rounded-lg font-medium
            whitespace-nowrap transition-all
            ${active === cat.id
              ? 'bg-blue-600 text-white shadow-md'
              : 'bg-white text-gray-700 hover:bg-gray-50'
            }
          `}
        >
          <span>{cat.icon}</span>
          <span>{cat.label}</span>
        </button>
      ))}
    </div>
  )
}
```

#### Step 2: ページコンポーネント更新

**app/(authenticated)/manual/page.tsx** を全面書き換え

#### Step 3: スタイリング

**globals.css**に追加:
```css
/* マニュアル専用スタイル */
.manual-content {
  @apply prose prose-lg max-w-none;
  @apply prose-headings:scroll-mt-20;
  @apply prose-h2:text-2xl prose-h2:font-bold prose-h2:mt-8 prose-h2:mb-4;
  @apply prose-h3:text-xl prose-h3:font-semibold prose-h3:mt-6 prose-h3:mb-3;
  @apply prose-p:leading-relaxed prose-p:text-gray-700;
  @apply prose-a:text-blue-600 prose-a:no-underline hover:prose-a:underline;
  @apply prose-code:bg-gray-100 prose-code:px-1.5 prose-code:py-0.5 prose-code:rounded;
  @apply prose-pre:bg-gray-900 prose-pre:text-gray-100;
  @apply prose-img:rounded-lg prose-img:shadow-md;
  @apply prose-table:border prose-table:border-gray-200;
  @apply prose-th:bg-gray-50 prose-th:font-semibold;
  @apply prose-td:border prose-td:border-gray-200;
}

/* 画像拡大機能 */
.manual-content img {
  @apply cursor-pointer transition-transform hover:scale-105;
}

/* 目次スタイル */
.toc {
  @apply sticky top-20 bg-white rounded-lg shadow p-4;
  @apply max-h-[calc(100vh-120px)] overflow-y-auto;
}

.toc-link {
  @apply block py-1 px-2 text-sm text-gray-600 hover:text-blue-600 hover:bg-blue-50 rounded;
}

.toc-link.active {
  @apply text-blue-600 bg-blue-50 font-medium;
}
```

### フェーズ3: 検索機能強化（1日）

#### Step 1: 検索ロジック改善

**lib/manual/search.ts** を拡張

#### Step 2: Q&A検索コンポーネント

**components/QA/QASearchBox.tsx** を作成

### フェーズ4: 画像対応（進行中）

#### Step 1: 画像ディレクトリ作成
```bash
mkdir -p public/images/manual/{login,qr,tools,attendance,documents,settings}
```

#### Step 2: 画像最適化スクリプト
```bash
# ImageMagickまたはSharpでリサイズ
npm install sharp
```

```javascript
// scripts/optimize-images.js
const sharp = require('sharp')
const fs = require('fs')
const path = require('path')

const sizes = {
  full: 1200,
  inline: 800,
  small: 400,
  mobile: 375
}

// 画像最適化処理
```

#### Step 3: Markdown画像記法の統一

**マニュアル内の画像記法を統一**:
```markdown
<img src="/images/manual/login/login-screen-pc.png"
     width="700"
     alt="ログイン画面（PC）"
     className="manual-image" />
```

---

## 技術的実装詳細

### MDファイル対応の確認

**現在の実装は既にMD形式に対応済み**:

```typescript
// lib/manual/metadata.ts (70行目)
} else if (item.endsWith('.mdx') || item.endsWith('.md')) {
  files.push(fullPath)
}
```

**MarkdownからHTML変換も実装済み**:

```typescript
// lib/manual/metadata.ts (12-18行目)
async function markdownToHtml(markdown: string): Promise<string> {
  const result = await remark()
    .use(remarkGfm)  // GitHub Flavored Markdown
    .use(remarkHtml, { sanitize: false })
    .process(markdown)
  return result.toString()
}
```

### スタイル適用方法

**現在のスタイル適用**:
```typescript
// app/(authenticated)/manual/[...slug]/page.tsx (112行目)
<article className="prose prose-blue max-w-none">
  <div dangerouslySetInnerHTML={{ __html: article.content }} />
</article>
```

**改善後のスタイル**:
```typescript
<article className="manual-content">
  <div dangerouslySetInnerHTML={{ __html: article.content }} />
</article>
```

---

## 実装優先順位

### 🔴 最優先（今週中）
1. ✅ データソース移行（`content/` → `docs/`）
2. ✅ Frontmatterの追加（全MDファイル）
3. ✅ 基本動作確認

### 🟡 高優先（来週）
4. 新デザイン実装（SearchBox, QuickStartCards, CategoryTabs）
5. Q&A検索機能
6. モバイル対応強化

### 🟢 中優先（2週間以内）
7. 画像15枚（優先度A）の準備
8. 画像最適化システム
9. 目次自動生成機能

### ⚪ 低優先（1ヶ月以内）
10. 画像25枚（優先度B, C）の準備
11. アクセス解析
12. フィードバック機能

---

## まとめ

### ✅ 実現可能性

**Yes, すべて実現可能です！**

1. **MDファイル対応**: ✅ 既に実装済み
2. **スタイル適用**: ✅ Tailwind CSSで実装可能
3. **検索機能**: ✅ クライアントサイド検索で実装可能
4. **画像表示**: ✅ Markdown/HTML混在で実装可能

### 📊 工数見積もり

- **フェーズ1（データ移行）**: 1-2日
- **フェーズ2（デザイン刷新）**: 2-3日
- **フェーズ3（検索機能）**: 1日
- **フェーズ4（画像対応）**: 進行中（画像準備次第）

**合計**: 4-6日（画像準備を除く）

### 🎯 次のアクション

1. この提案書を確認・承認
2. Frontmatter追加スクリプトの実行承認
3. 優先度Aの画像15枚のスクリーンショット撮影開始
4. フェーズ1の実装開始

---

**最終更新日**: 2026-01-22
